routing: ${routingMQ:rabbit.pay.#}
queue: ${queueMQ:rabbit-queue}
exchange: ${exchangeMQ:rabbit-exchange}
delayed: ${exchangeMQ:rabbit-delayed}
subscription: ${subscriptionMQ:rabbit.subscription}
password: ${passwordMQ:rabbit-queue-delayed}
username: ${usernameMQ:guest}
dead-assign: ${deadpayMQ:rabbit.pay.delayed}